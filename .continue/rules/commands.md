## Running Commands

- Always activate the python virtual environment with `source .venv/bin/activate` before running any commands.
- Do not use the `streamlit` command to run the app - neither for the purpose of testing nor any other purpose.
